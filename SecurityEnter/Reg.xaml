<Window x:Class="SecurityEnter.Reg"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SecurityEnter"
        mc:Ignorable="d"
        Title="Reg" Height="150" Width="300" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Логин:"/>
        <TextBox Grid.Row="0" Grid.Column="1" Name="LoginTextBox"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Пароль:"/>
        <PasswordBox Grid.Row="1" Grid.Column="1" Name="PasswordBox"/>

        <Button Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Content="Назад" Click="Button_Click"/>
        <Button Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Content="Зарегистрироваться" Click="Button_Click_1"/>
    </Grid>
</Window>
